<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:background="@color/colorAccent"
    android:gravity="bottom"
    android:padding="6dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <!-- android:gravity="bottom"    cuz we want everything to be pasted at the BOTTOM of this header -->
    <!-- android:theme="@style/ThemeOverlay.AppCompat.Dark"    make the text WHITE, as if everything in the background was dark. Preview won't show it white, but upon running it will be white-->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher_round"/>


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:text="Ceva altceva"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Body1"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="info@cevaceva.com" />





</LinearLayout>